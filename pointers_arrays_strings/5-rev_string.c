#include "main.h"
#include <stdio.h>

void
rev_string(char *s)
{
int n;
int a;
char mario[1000];

n = 0;
for (n = 0; s[a]; a++)
{
	continue;
}
n -= 1;
for (; s[a]; n--)
{
	mario[a] = s[n];
	a++;
}
for (n = 0; s[n]; n++)
	s[a] = mario[a];
}
