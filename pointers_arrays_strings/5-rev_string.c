#include "main.h"

void
rev_string(char *s)
{
int n;
int a;
char mario;

n = 0
for (n = 0; s[a]; a++)
{
	continue;
}
n -= 1;
for (; s[a]; n--)
{
	mario[a] = s[n];
	a++;
}
for (n = 0; s[n]; n++)
	s[a] = mario[a];
}
